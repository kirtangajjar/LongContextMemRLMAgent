# AGENTS.md

## Generic execution learning rule
- If a user asks to *perform* a concrete action (for example: download a benchmark, run an evaluation, execute a command, collect results), do the action directly first and report outcomes.
- Only add new reusable scripts/files when the user explicitly asks for automation or when direct execution is not feasible; otherwise keep repository changes minimal.
